(()=>{


    enum AudioLevel {
    min = 1,
    medium,
    max = 10
}

    let currentAudio = AudioLevel.medium;

    console.log( currentAudio);


})()

//enum to manage numbers, each element is in order and has de posicion since 1.